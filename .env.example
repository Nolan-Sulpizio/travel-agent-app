# Travel Agent App Environment Configuration
# Copy this file to .env and fill in your values
# NEVER commit .env to version control

# ===========================================
# REQUIRED CONFIGURATION
# ===========================================

# n8n Webhook URL for the travel agent workflow
# Get this from your n8n instance: Workflows > Your Workflow > Webhook Trigger > Production URL
VITE_WEBHOOK_URL=https://your-n8n-instance.app.n8n.cloud/webhook/your-webhook-id/chat

# ===========================================
# OPTIONAL CONFIGURATION
# ===========================================

# App title override (defaults to "Travel Agent")
# VITE_APP_TITLE=My Custom Travel Planner

# Enable debug logging (set to "true" to enable)
# VITE_DEBUG=false

# Analytics tracking ID (if using)
# VITE_ANALYTICS_ID=

# ===========================================
# NOTES
# ===========================================
# 
# Vite exposes env vars prefixed with VITE_ to the client
# Access in code via: import.meta.env.VITE_WEBHOOK_URL
#
# For local development, you can also create:
# - .env.local (local overrides, not committed)
# - .env.development (dev-specific settings)
# - .env.production (production settings)
